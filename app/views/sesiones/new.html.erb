<h1>Iniciar Sesion</h1>

<%= flash[:error] %>

<%= form_for :sesion, url: '/sesiones' do |f| %>
	<%= f.label :mail %>
	<%= f.text_field :mail %>

	<%= f.label :clave %>
	<%= f.password_field :clave %>

	<%= f.submit 'Iniciar Sesión' %>
<%end%>

<p>Find me in app/views/sesiones/new.html.erb</p>
